# 辩论手记 · 使用说明

一个只在你自己电脑上运行的中文辩论记录程序。不需要登录，不联网，所有数据都存在本机。

## 一、安装与启动

交付目录（`辩论程序\交付\`）里有两种版本，任选其一：

| 文件 | 说明 |
| --- | --- |
| `辩论手记-安装版-1.0.0.exe` | 安装版：双击安装，可选安装位置，会创建桌面和开始菜单快捷方式 |
| `辩论手记-免安装版-1.0.0.exe` | 免安装版：双击即用，不写注册表、不建快捷方式，适合放 U 盘 |

首次启动会自动完成三件事：

1. 建立数据目录（默认 `%APPDATA%\DebateNotes`）
2. **写入一批示例数据**（10 场比赛 / 3 个赛事 / 2 条荣誉）并把两张参考照片放进照片库
3. 为当天做一份自动备份

示例数据是让你一打开就能看到五个页面的完整效果（统计、日历排布、归组、荣誉、履历都会立刻有内容）。带「示例」标记的记录可以随时在「设置 → 示例数据」里处理：

- **删除全部示例数据**：一次性清掉带「示例」标记的比赛、赛事与荣誉；你自己录入的记录、以及照片库里的照片都不动。
- **重新写入示例数据**：删掉之后想再看到示范效果，点这个按钮就能按**当天**的日期补回一份（10 场比赛 / 3 个赛事 / 2 条荣誉，赛期跟着当天重新计算）。它是幂等的——已经存在的示例按名字与辩题跳过，不会重复写入，可以反复点；你自己录入的内容也完全不动。示例只会在首次启动时自动写一次，删掉后重启不会再自己冒出来。

程序默认**不显示任何背景照片**，就是干净的暖米色底（示例照片只是放进照片库里，不会自己当背景）。想用照片当背景，在「设置 → 背景照片」里点某张卡片切过去就行，也可以直接添加你自己的图。以后每次启动，只在**当天第一次**打开且已有数据时做自动备份，最多保留最近 30 份。

## 二、五个页面

- **战绩总览**：已完成场次、胜场、负场、无胜负、胜率、单场最佳辩手数、赛事荣誉、未出结果、待赛。
  可切换「本月 / 今年 / 全部 / 自选日期」，可切换「正赛 / 模拟赛 / 全部」，可按赛事筛选；下面是类别对照、赛事成绩、最近比赛、时间范围内的荣誉。
- **比赛记录**：分「比赛 / 赛事 / 荣誉」三个页签。比赛可增删改查、按辩题或赛事搜索；新增/编辑时有日期、开赛时间、辩题、正赛或模拟赛、所属赛事、正反方、辩位、比赛结果、是否最佳辩手、简评。删除前会二次确认。
  赛事页可以把多场比赛归到一个赛事下，并填赛事成绩（冠军 / 亚军 / 四强…也可自己写）。荣誉页记录荣誉名称和获得日期。
- **比赛日历**：月视图和周视图。点某一天直接新增比赛（日期已填好），点日历里的比赛可以查看或编辑；正赛与模拟赛用颜色区分；周视图按开赛时间排列，没填时间的比赛单独放在下面一块。支持跨月查看、回到今天、同一天多场比赛。
- **履历**：自动汇总参赛经历、赛事成绩、荣誉（单场最佳辩手自动统计，不用手填）。选好时间范围后可一键复制履历文字，纯文本和 Markdown 两种格式都有。
  **可以自己编辑**：点「编辑履历」进入编辑框，「用自动汇总内容填充」先把自动内容填进去，再按自己的语气改，保存后复制按钮复制的就是你写的那份；随时可以「恢复自动生成」回到自动汇总。手动内容与时间范围无关，重启也在。
- **设置**：外观皮肤、背景照片与轮播、示例数据、可扩展选项、自动备份、导出与恢复、数据目录（可搬到 D 盘）、打开数据目录。

## 三、外观皮肤（四套，随时切换）

设置页最上面的「外观皮肤」里可以点选，切换后立即生效并记住，换皮肤**只改配色与装饰，不改页面布局、字号和数据结构**。

| 皮肤 | 风格 |
| --- | --- |
| 极简手账 | 默认。暖米底色、低饱和木色强调，像纸质手账 |
| 极简白 | 纯白面板、细灰描边、直角、无阴影，信息密度最高 |
| 中式雅致 | 宣纸底色、朱红与墨色、宋体正文、回纹双线描边与题头装饰 |
| 水墨夜色 | 深色底、金铜色强调，夜里看不刺眼 |

四套皮肤下正文、统计数字与面板标题的对比度都经过自动检查（≥ WCAG AA 的 4.5:1），所以在任何一套皮肤下文字都清楚。

## 四、背景照片（默认不显示，按需开启）

默认没有显示任何背景照片，页面是干净的底色。想换成照片背景时：

- **批量添加**：设置 → 背景照片 → 添加照片，可一次选多张。
- **支持的图片格式**：JPG / JPEG / JPE / JFIF / PNG（含 APNG）/ WebP / GIF / BMP / AVIF / SVG / ICO，另外 HEIC / HEIF / TIFF / TIF 会先自动转成 PNG 再入库（前提是系统自带解码器能读，Windows 装了「HEIF 图像扩展」就能读 iPhone 的 HEIC）。
  格式按**文件内容**识别而不是扩展名，所以 `.jfif`、没有扩展名、甚至后缀写错的图片也能正常导入。
- **导入结果有明确提示**：一次导入里哪些成功、哪些被跳过、为什么被跳过（例如「不是可识别的图片文件」「本机无法解码，请先另存为 JPG/PNG」）都会在右下角提示里写清楚，不会静默丢文件。
- **照片会被复制**进数据目录，原文件之后移走或删掉都不影响显示。
- **切换**：手动点某张卡片切换；也可以开启轮播，默认每 5 分钟换一张，间隔可调（10 秒 ~ 24 小时），可暂停。
- **删除**：单张删除，或「清空全部」。清空后重启也不会自己冒出来。
- 照片只是背景图层，不改变页面布局；内容面板保持足够不透明，文字始终清楚。

### 4.1 单独调整每一张照片（位置 / 大小 / 透明度）

点照片卡片上的「**调整**」，会打开一个所见即所得的调整窗口（预览框与窗口同比例）：

- **位置**：按住预览直接拖动，或拉「水平位置 / 垂直位置」滑块；**铺满模式下怎么拖都不会露出空白**（只在照片多出来的那部分里移动）。
- **大小**：1.00× ~ 4.00× 绕中心缩放；鼠标放在预览上**滚滚轮也能缩放**。
- **不透明度**：0 ~ 100%，越低照片越淡、面板上的文字越清楚。
- **模糊**：0 ~ 24 px，轻微模糊能让正文更清楚。
- **显示方式**：铺满裁切 / 完整显示。铺满＝照片盖满窗口（不露白）；完整显示＝整张照片放进窗口、四周可能留白，这时可以把它摆在框内任意位置。
- 改动**立即生效并自动保存**，随时关掉窗口都不会丢；「恢复默认」回到跟随全局设置，「套用到其他照片」把当前这套调整复制给其余照片。
- 卡片上会显示「已调整」标记；轮播或手动切换照片时，每张照片都用自己那套调整。

## 五、统计口径

- 状态是**单字段五值**：待赛 / 未出结果 / 胜 / 负 / 无胜负。
- **已完成场次** = 胜 + 负 + 无胜负 + 未出结果；**待赛不计入已完成**。
- **胜率 = 胜场 ÷（胜场 + 负场）**。「无胜负」和「未出结果」不进分母；没有有效胜负时显示「暂无」。总览的「类别对照」每行右侧会直接写出算式（例如 `3 ÷（3 + 1）` = 75.0%），一眼能核对，不会出现「已完成 5 场为什么是 75%」的疑惑。
- 总览默认把正赛和模拟赛一起统计，可切换只看其中一类。
- 赛事荣誉（按获得日期计入时间范围）与单场最佳辩手分开统计，互不混淆。

## 六、备份与恢复

- **自动备份**：每天首次启动且已有数据时自动做一份，保留最近 30 份（可在设置里关掉或改保留数量）。
- **手动导出**：设置 → 备份 → 导出完整备份，得到一个 `.zip`，里面包含数据库、照片和设置，可以直接复制到别处保存。
- **恢复**：选一个导出的 zip 恢复。恢复前程序会**自动先给当前数据做一份安全备份**，所以恢复错了还能退回来。
- **损坏的 zip 不会破坏现有数据**：程序会先校验（结构、校验和、清单），校验不通过就只报错、不改动任何现有数据。

## 七、数据在哪里

默认在 `%APPDATA%\DebateNotes`：

```
database.sqlite   比赛、赛事、荣誉等数据
settings.json     界面与设置（含皮肤、背景、统计范围）
photos\           背景照片（已复制进来的副本）
backups\          自动备份与安全备份（.zip）
seed.json         示例数据写入标记（只写一次，删掉它会让程序下次启动重新判断）
runtime\          Chromium 运行时缓存（纯缓存，可以随便删）
tmp\              临时文件
```

在「设置」里点「打开数据目录」可以直接打开这个文件夹。

### 把数据放到别的盘（比如 D 盘）

数据目录可以整体搬到 C 盘以外，步骤：

1. 「设置 → 数据与关于」点「**更改数据目录…**」，选一个文件夹（例如 `D:\辩论手记数据`，没有就先新建）。
2. 确认框里会写明将要把数据复制到哪里；点「确认切换」后，程序会把比赛记录、赛事、荣誉、照片、备份、设置**整套复制过去**。
3. 弹出「数据目录已切换」后点「**立即重启**」（或者稍后自己重启），重启之后就固定用新位置了。
4. 界面上的「数据目录」旁边会显示「**自定义位置**」标记；原位置会保留一份老数据，确认新位置没问题后可以自己删掉它。

几个细节：

- 如果选的文件夹里**已经有**辩论手记的数据（有 `database.sqlite`），程序会**直接改用它**，不会复制、也不会覆盖你当前的数据 —— 适合你之前已经把数据拷到 D 盘的情况。
- 想搬回默认位置，点「**恢复默认位置**」即可（同样会把数据复制回 `%APPDATA%\DebateNotes`）。
- 选择的位置由一个很小的指针文件 `%APPDATA%\DebateNotes\location.json` 记住；只搬迁数据本身，缓存目录（`runtime`）不搬。
- 备份 zip 跟着数据目录走，所以搬完之后「备份与恢复」里看到的还是同一批备份。

- **换电脑**：在新电脑装好程序后，用导出的 zip 恢复即可（比赛、赛事、荣誉、照片、设置都会回来）。
- **从旧版本升级**：直接用新版本覆盖安装（或换新的免安装版）就行，程序首次启动会自动升级数据结构，比赛记录、赛事、荣誉、照片与设置都不会动；照片的新调整项会先按「跟随全局设置」处理。
- **卸载程序不会删除数据目录**，需要清理时手动删掉上面这个文件夹。
- 也可以设置环境变量 `DEBATE_NOTES_DATA_DIR` 指向目标目录再启动程序（优先级高于上面的设置，适合绿色版便携使用）。

## 八、界面截图

- `docs\screenshots\界面\`：五个页面与四套皮肤的成品截图（`08-皮肤-*.png`），外加 `12-履历-手动编辑.png`、`13-照片调整-位置与大小.png`
- `docs\screenshots\验收\`：验收测试过程中自动产出的截图（含不同窗口尺寸、备份列表、照片调整弹窗等）
